<odoo>
    <record id="purchase_order_form_view" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='button_confirm']" position="attributes">
                <attribute name="name">action_confirm_order_wizard</attribute>
                <attribute name="type">object</attribute>
            </xpath>
        </field>
    </record>

    <menuitem id="menu_create_appointment"
              name="Create Appointment"
              parent="menu_purchase_root"
              action="action_create_appointment"
              sequence="20"/>
</odoo>
